# PyLot

Python 脚本调度桌面管理系统，适合需要定期运行多个 Python 脚本的场景。

## 功能

- 用可新增、重命名、排序和删除的列表管理 Python 脚本；宽屏采用“列表 / 脚本 / 详情”三栏布局，窄屏自动切换为列表与详情两级页面
- 支持 script、module 和 custom 三种启动模式，自动识别虚拟环境解释器
- 脚本和服务工作目录可直接在 VS Code 中打开
- 实时日志输出（UTF-8，支持中文）
- cron 定时调度，脚本和工作流均可设置；支持在定时任务总览直接新增/编辑、自定义 cron 多行导入、重复触发冲突提示、按最近运行时间筛选排序、查看最近运行情况和脚本历史日志
- 全局 .env + 脚本私有环境变量双层合并
- 卡死超时检测与强杀
- **Workflow**：拖拽编排多脚本依赖关系，支持并发执行和错误终止
- **自定义命令**：脚本可选择 custom 模式，仅配置工作目录和完整命令，适合 `python -m package subcommand --flag` 这类入口
- **服务管理**：管理长期运行的本地命令/服务，支持启动、停止、可靠重启、访问链接、端口占用检测、实时日志和状态追踪
- **服务跟随 PyLot 启动**：服务可单独开启“跟随 PyLot 启动”，软件启动后自动拉起对应服务
- **飞书通知**：脚本或工作流执行结束后通过 lark-cli 发送飞书消息
- 关闭窗口最小化到系统托盘（右键菜单：定时任务 / 退出），有脚本运行时图标变色
- 深色/浅色主题切换，字体可选，窗口大小记忆
- 单实例运行（再次启动时唤起已有窗口）

## 技术栈

Go + [Wails v2](https://wails.io) + Vue3 + SQLite（纯 Go 驱动，无需 GCC）

## 交互与可靠性

- 统一深色/浅色设计系统、可搜索脚本栏和应用内确认弹窗
- 脚本日志支持搜索、仅看错误、自动滚动、历史记录和可折叠面板
- 设置、服务、定时任务和工作流编辑页不显示无关的全局脚本输出
- 保存、运行、停止、启停和删除操作提供即时等待状态并阻止重复提交
- 未保存的脚本、工作流、服务或设置在离开页面前会提示确认
- 工作流支持真实停止、结构校验、运行态恢复和按节点聚合日志
- 删除脚本/工作流会校验引用并清理关联定时任务，scheduler 注册失败会回滚
- SQLite 写入路径对脚本日志和运行状态做串行化与短重试，降低多脚本并发运行时的 `database is locked` 风险

详细页面职责和交互约定见 [UI 与交互规范](./docs/ui-interaction-spec.md)。

## 开发

```bash
# 安装依赖
go mod download
cd frontend && npm install && cd ..

# 开发模式（热重载）
wails dev

# 打包
wails build -platform windows/amd64 -ldflags "-H windowsgui"
```

详细环境搭建见 [CLAUDE.md](./CLAUDE.md)。

## 要求

- Windows 10/11
- WebView2（系统自带）
- Python 解释器路径在 UI 中配置
- 飞书通知可选：需安装 `lark-cli` 并在设置页填入路径和 Open ID

## 服务管理

服务用于托管需要长期运行的本地进程，例如本地 API、爬虫守护进程、数据同步服务或前端开发服务。服务命令支持 Windows 命令行引号解析，工作目录可作为相对路径基准目录。

在“服务”页可以：

- 新增服务：填写名称、命令、工作目录，可选配置 HTTP/HTTPS 访问端口，并选择是否跟随 PyLot 启动
- 控制服务：启动、停止、重启、删除
- 快速访问：配置端口后生成 `http(s)://127.0.0.1:{port}` 链接，可直接在浏览器打开
- 查看状态：运行状态、启动进程 PID、端口监听进程及其 PID、进程树归属、启动时间、停止时间、最近错误
- 打开目录：从服务编辑页或详情页直接在 VS Code 中打开工作目录
- 处理冲突：启动前检测 TCP 监听端口；占用时展示进程名、路径和 PID，经确认并再次核对 PID 后可结束占用进程并启动服务
- 查看日志：服务运行时 stdout/stderr 实时输出，最近日志会保留在本次 PyLot 会话内
- 切换自启：在服务详情中直接打开或关闭“跟随 PyLot 启动”

端口配置只用于访问入口和占用检测，不会自动改写服务命令。服务仍需通过命令参数、环境变量或自身配置监听对应端口。重启时 PyLot 会等待原进程退出及端口释放，避免固定延时导致的重复绑定失败。

## 定时任务总览

“定时任务”页用于集中查看和维护脚本/工作流调度规则。任务列表默认显示即将运行的启用任务，并按下次运行时间从近到远排序；可通过“今日运行”“已停止”“全部”筛选快速定位不同状态的规则。

页面下半部分显示最近运行情况，脚本和工作流会合并成一条时间线；选择脚本运行记录后，可直接查看该次运行保存的日志输出。

## 飞书通知配置

1. 安装 lark-cli：`npm install -g @larksuiteoapi/lark-cli`
2. 初始化：`lark-cli config init`（填入 App ID 和 App Secret）
3. 在 PyLot 设置页填入 lark-cli 路径和你的 Open ID

## 运行态恢复

PyLot 启动和退出时会自动收敛脚本运行态：将异常中断遗留的 `running` 运行记录标记为 `killed`，并清空 `running_tasks`，避免历史日志长期显示为“运行中”。

## 自定义命令模式

`custom` 启动模式用于无法用单个 `.py` 文件或 module 路径表达的入口。该模式只需要填写工作目录和命令，命令按 Windows 命令行规则解析，支持带空格路径和引号参数；相对可执行文件会优先按工作目录解析。

示例：

```powershell
工作目录：W:\WorkSpace\nav_data_tracking
命令：.\.venv\Scripts\python.exe -m fof99_nav_ingestion run --include-stale --update-token
```
